---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: elasticlog
    component: elastic
  name: elastic-svc
spec:
  ports:
  - port: 9200
    protocol: TCP
    targetPort: 9200
  selector:
    app: elasticlog
    component: elastic
  type: ClusterIP
